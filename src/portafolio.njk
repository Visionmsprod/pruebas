---
layout: "base.njk"
title: "Portafolio"
description: "Explora nuestro portafolio de fotografía y video. Bodas, quince años, retratos, eventos, fotografía de producto y corporativa."
---

<style>
    /* ... (Todos tus estilos del portafolio que ya tenías van aquí, sin cambios) ... */
    .portfolio-filters { text-align: center; margin-bottom: 40px; }
    .portfolio-filters button { background: #e0e0e0; color: #333; border: none; padding: 10px 20px; margin: 5px; border-radius: 5px; cursor: pointer; font-weight: 600; transition: background-color 0.3s; }
    .portfolio-filters button:hover { background-color: #ccc; }
    .portfolio-filters button.active { background-color: var(--color-rojo-principal); color: white; }
    .portfolio-gallery { column-count: 3; column-gap: 1em; }
    .portfolio-item { display: inline-block; margin: 0 0 1em; width: 100%; overflow: hidden; border-radius: 5px; cursor: pointer; }
    .portfolio-item img { width: 100%; height: auto; display: block; transition: transform 0.3s ease; }
    .portfolio-item:hover img { transform: scale(1.05); }
    .lightbox { display: none; position: fixed; z-index: 9999; top: 0; left: 0; width: 100%; height: 100%; background-color: rgba(0, 0, 0, 0); justify-content: center; align-items: center; transition: background-color 0.3s ease-in-out; }
    .lightbox.active { display: flex; background-color: rgba(0, 0, 0, 0.85); }
    .lightbox-content { max-width: 90%; max-height: 90%; position: relative; transform: scale(0.5); opacity: 0; transition: transform 0.3s ease-in-out, opacity 0.3s ease-in-out; }
    .lightbox.active .lightbox-content { transform: scale(1); opacity: 1; }
    .lightbox-content img { width: auto; height: auto; max-width: 100%; max-height: 90vh; object-fit: contain; border-radius: 5px; }
    .lightbox-close { position: absolute; top: -45px; right: -10px; color: white; font-size: 3rem; font-weight: bold; cursor: pointer; transition: transform 0.2s; text-shadow: 0 0 10px black; }
    .lightbox-close:hover { transform: scale(1.2); }
    @media (max-width: 991px) { .portfolio-gallery { column-count: 2; } }
    @media (max-width: 768px) { .portfolio-gallery { column-count: 1; } }
</style>

<main>
    <section class="page-header" style="background-image: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('/images/evento.jpg');">
        <div class="container"><h1>Nuestro Trabajo</h1></div>
    </section>
    <section id="portfolio-page">
        <div class="container">
            <div class="portfolio-filters">
                <!-- ... (Los botones de filtro no cambian) ... -->
            </div>
            <div class="portfolio-gallery">
                {# --- LÍNEA CORREGIDA --- #}
                {% for item in collections.all | getFilteredByTag("portafolio") %}
                <div class="portfolio-item" data-category="{{ item.data.category }}">
                    <img src="{{ item.data.image | url }}" alt="{{ item.data.title }}">
                </div>
                {% endfor %}
            </div>
        </div>
    </section>
</main>

<!-- Lightbox HTML Structure (se mantiene igual) -->
<div class="lightbox" id="lightbox">
    <span class="lightbox-close" id="lightbox-close">×</span>
    <div class="lightbox-content">
        <img src="" id="lightbox-img" alt="Imagen Ampliada">
    </div>
</div>

<script>
    // El script del lightbox y los filtros se moverá a main.js, 
    // pero lo dejamos aquí temporalmente para asegurar que funcione.
</script>
```</details>
